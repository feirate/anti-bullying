# 环境变量配置示例
# 复制此文件为.env并填入实际值

# Supabase配置
# 注意：前端只能使用匿名密钥(anon key)，不要使用服务密钥(service key)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# 安全提醒：
# 1. 前端环境变量会暴露给用户，不要存储敏感信息
# 2. 确保.env文件已被.gitignore排除
# 3. 生产环境使用构建时注入或meta标签方式配置

# 其他配置
# NODE_ENV=development
# DEBUG=true